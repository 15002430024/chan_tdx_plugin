# chan_tdx_plugin v7.5.1 修改提示词文档

> 配合《修改需求文档 v7.5.1》使用
> 每个 Step 完成后需编译验证 + 运行单测，通过后再进入下一步
> Step 编号中标注 `[临时]` 的，会在后续步骤中被吸收/删除

---

## Phase 0：回归基线采集（Phase 1 之前必须完成）

### Step 0.1 — 采集当前版本输出基线

**目的**：在任何代码修改之前，保存当前 live DLL 的计算结果作为回归比对基准。

**操作**：

1. 打开通达信，加载当前 `chan.dll`（v7.4）
2. 分别打开 999999 日线 和 600519 5分钟图
3. 对每个样本，用"数据导出"或诊断公式，保存以下数组到 CSV：

```
文件名格式：baseline_<代码>_<周期>.csv

列结构：
bar_index, BI, ZSZG, ZSZD, BSIG, SSIG, KXG, KXD

其中：
  BI   = TDXDLL1(2, H, L, C)   笔端点
  ZSZG = TDXDLL1(3, H, L, C)   中枢高
  ZSZD = TDXDLL1(4, H, L, C)   中枢低
  BSIG = TDXDLL1(7, H, L, C)   买点
  SSIG = TDXDLL1(8, H, L, C)   卖点
  KXG  = TDXDLL1(20, H, L, C)  顶端点价格
  KXD  = TDXDLL1(21, H, L, C)  底端点价格
```

4. 保存到 `test/baseline/` 目录

**如果不方便通过通达信导出**，替代方案是在 `FullAnalyzeWithMA` 末尾临时添加日志代码，把所有非零的 BI / ZSZG / BSIG / SSIG 打到 `chan_debug.log`，然后手工整理成 CSV。

### Step 0.2 — 打 Git tag

```bash
git tag v7.4-final -m "回归基线版本，Phase 1 开始前快照"
git push origin v7.4-final
```

同时备份当前 DLL：

```powershell
copy T0002\dlls\chan.dll backup\chan_v74.dll
```

---

## Phase 1：架构收敛（P0 级）

### Step 1.1 — 禁用 FullAnalyzeWithMA 粗缓存

**目标**：消除实时行情下尾 bar 不刷新的问题

**操作文件**：`src/tdx_standard.cpp`

**具体修改**：找到 `FullAnalyzeWithMA` 函数开头的缓存判断，注释掉整段：

```cpp
// 修改前：
static void FullAnalyzeWithMA(const float* highs, const float* lows, const float* closes, int count) {
    if (count == g_LastCount && count > 0 &&
        highs[0] == g_LastHigh0 && lows[0] == g_LastLow0) {
        return;
    }
    ...
}

// 修改后：
static void FullAnalyzeWithMA(const float* highs, const float* lows, const float* closes, int count) {
    // [v7.5] 暂时禁用缓存，确保每次调用都重算
    // TODO: 后续恢复时至少比较 count + highs[count-1] + lows[count-1] + closes[count-1]
    // if (count == g_LastCount && count > 0 &&
    //     highs[0] == g_LastHigh0 && lows[0] == g_LastLow0) {
    //     return;
    // }
    ...
}
```

**验证**：编译通过，无新增警告。

---

### Step 1.2 [临时补丁] — 接通 CZSC.ini 到主 DLL

> **注意：本步骤是临时补丁。Step 1.3 完成后，这里的 `LoadConfig()` 仍保留，但 `CheckBI(g_MinBiLen)` / `CheckZS(g_MinZsBiCount)` 调用会被 `g_Core.SetConfig() + g_Core.Analyze()` 替代。**

**目标**：让 `min_bi_length` 和 `min_zs_bi_count` 从配置文件读取

**操作文件**：`src/tdx_standard.cpp`

**配置键名约定**（全局锁定，不可变更）：

| INI 键 | 代码变量 | ChanConfig 字段 | 默认值 |
|--------|----------|-----------------|--------|
| `min_bi_length` | `g_MinBiLen` | `min_bi_len` | 5 |
| `min_zs_bi_count` | `g_MinZsBiCount` | `min_zs_bi_count` | 3 |

**具体修改**：

1. 在文件顶部 include 区域后添加配置读取逻辑：

```cpp
// [v7.5] 简易配置读取
static int g_MinBiLen = 5;
static int g_MinZsBiCount = 3;
static bool g_ConfigLoaded = false;

static void LoadConfig() {
    if (g_ConfigLoaded) return;
    g_ConfigLoaded = true;

    // 获取 DLL 所在目录
    char dllPath[MAX_PATH] = {0};
    HMODULE hMod = NULL;
    GetModuleHandleExA(GET_MODULE_HANDLE_EX_FLAG_FROM_ADDRESS,
                       (LPCSTR)&LoadConfig, &hMod);
    GetModuleFileNameA(hMod, dllPath, MAX_PATH);

    // 替换文件名为 CZSC.ini
    char* lastSlash = strrchr(dllPath, '\\');
    if (lastSlash) {
        strcpy(lastSlash + 1, "CZSC.ini");
    }

    // ⚠️ 键名必须与 CZSC.ini 完全一致：min_bi_length, min_zs_bi_count
    g_MinBiLen = GetPrivateProfileIntA("General", "min_bi_length", 5, dllPath);
    g_MinZsBiCount = GetPrivateProfileIntA("General", "min_zs_bi_count", 3, dllPath);

    char logMsg[256];
    sprintf(logMsg, "[v7.5] Config: min_bi_length=%d, min_zs_bi_count=%d, path=%s",
            g_MinBiLen, g_MinZsBiCount, dllPath);
    WriteLog(logMsg);
}
```

2. 在 `FullAnalyzeWithMA` 函数体内，替换硬编码调用：

```cpp
// 修改前：
    CheckBI(5);
    CheckZS(3);

// 修改后（临时，Step 1.3 后会被替代）：
    LoadConfig();
    CheckBI(g_MinBiLen);
    CheckZS(g_MinZsBiCount);
```

3. 确认 `CZSC.ini` 中对应节和键名存在：

```ini
[General]
min_bi_length=5
min_zs_bi_count=3
```

**验证**：
- 编译通过
- 启动通达信后检查 `D:\chan_debug.log`，确认打印 `[v7.5] Config: min_bi_length=5, min_zs_bi_count=3`
- 修改 `CZSC.ini` 中 `min_bi_length=7`，重启后日志应显示 7，且图上笔数明显减少

---

### Step 1.3 — 统一算法到 chan_core.cpp（最终结构）

> **这是本轮最大改动。完成后 `tdx_standard.cpp` 将不含任何算法逻辑。**
> **开始前确保 Step 0.2 的 Git tag 和 DLL 备份已完成。**

#### Step 1.3a — 修改 CMakeLists.txt

让 `chan.dll` 链接 `chan_core.cpp`：

```cmake
# 修改前（大致形式）：
add_library(chan SHARED src/tdx_standard.cpp src/dllmain.cpp src/logger.cpp)

# 修改后：
add_library(chan SHARED
    src/tdx_standard.cpp
    src/chan_core.cpp
    src/dllmain.cpp
    src/logger.cpp
)
target_include_directories(chan PRIVATE ${CMAKE_SOURCE_DIR}/include)
```

编译验证：先确保能编译通过（可能有符号冲突，下一步解决）。

---

#### Step 1.3b — 在 tdx_standard.cpp 中引入 ChanCore

文件顶部添加：

```cpp
#include "chan_core.h"

// [v7.5] 全局 ChanCore 实例，替代所有独立算法函数和全局 vector
static chan::ChanCore g_Core;
```

---

#### Step 1.3c — 重写 FullAnalyzeWithMA（吸收 Step 1.2）

用 `g_Core` 替代全部手工调用：

```cpp
static void FullAnalyzeWithMA(const float* highs, const float* lows, const float* closes, int count) {
    // [v7.5] 缓存暂时禁用（见 Step 1.1）

    // 加载配置（从 CZSC.ini）
    LoadConfig();

    // 将配置注入 ChanCore
    chan::ChanConfig cfg = g_Core.GetConfig();
    cfg.min_bi_len = g_MinBiLen;
    cfg.min_zs_bi_count = g_MinZsBiCount;
    g_Core.SetConfig(cfg);

    // 一次性完整分析（去包含 + 分型 + 笔 + 中枢）
    g_Core.Analyze(const_cast<float*>(highs),
                   const_cast<float*>(lows),
                   const_cast<float*>(closes),
                   nullptr,  // volumes
                   count);

    // MA 均线仍在本层计算（ChanCore 不含 MA 逻辑）
    g_Closes.resize(count);
    for (int i = 0; i < count; ++i) g_Closes[i] = closes[i];
    CalcMA(closes, count, 13, g_MA13);
    CalcMA(closes, count, 26, g_MA26);
}
```

此步完成后，Step 1.2 中的 `CheckBI(g_MinBiLen); CheckZS(g_MinZsBiCount);` 调用被完全替代。

---

#### Step 1.3d — 删除 tdx_standard.cpp 中所有算法函数体

删除以下**完整函数定义**（不是注释，是整段删除）：

```
RemoveInclude()
CheckFX()
EmitStroke()
CheckBI()
CheckZS()
```

同时删除对应的全局变量声明：

```
g_MergedKLines
g_Fractals
g_Strokes
g_Pivots
```

以及所有本地结构体定义（`MergedKLine`、`Fractal`、`Stroke`、`Pivot`）。这些结构体统一由 `#include "chan_types.h"` 或 `#include "chan_core.h"` 提供。

> **⚠️ 关键**：删除本地 struct 定义后，`Stroke.is_confirmed` 字段只存在于 `chan_types.h` 一处。确认 `chan_types.h`（或 `chan_core.h`）中的 `Stroke` 已包含 `is_confirmed` 字段。如果没有，先在 `chan_types.h` 中添加。

---

#### Step 1.3e — 迁移 GetBiSequence + 买卖点函数到 chan_core.cpp

将以下函数从 `tdx_standard.cpp` 迁移到 `chan_core.cpp`（作为 `ChanCore` 的成员方法或同文件内的静态函数）：

```
GetBiSequence()
CheckFirstBuy() / CheckSecondBuy() / CheckThirdBuy()
CheckFirstSell() / CheckSecondSell() / CheckThirdSell()
CheckPreFirstBuy() / CheckPreSecondBuy() / CheckPreThirdBuy()
CheckPreFirstSell() / CheckPreSecondSell() / CheckPreThirdSell()
CheckFirstBuyKJA() / CheckFirstBuyKJB()
```

迁移后，`tdx_standard.cpp` 中的 `BuySignal()` 和 `SellSignal()` 导出函数改为调用 `g_Core` 上的方法。

**如果 ChanCore 已有 `OutputBuySignal()` / `OutputSellSignal()` 方法**（从 `chan_core.cpp` 现有代码来看可能已有部分），则直接复用，只需确保逻辑与当前 `tdx_standard.cpp` 完全一致。

**如果逻辑不一致**，以 `tdx_standard.cpp` 当前逻辑为准（因为它是经过 v7.4 实战验证的版本），迁移时保持 1:1 复制。

---

#### Step 1.3f — 导出函数改用 g_Core getter

对所有 22 个导出函数，将直接访问全局 vector 改为通过 `g_Core` 的 getter：

| 原代码 | 改为 |
|--------|------|
| `g_Fractals` | `g_Core.GetFractals()` |
| `g_Strokes` | `g_Core.GetStrokes()` |
| `g_Pivots` | `g_Core.GetPivots()` |
| `g_MergedKLines` | `g_Core.GetMergedKLines()` |

示例（`FenXing`）：

```cpp
void FenXing(int DataLen, float* pfOUT, float* pfINa, float* pfINb, float* pfINc) {
    FullAnalyzeWithMA(pfINa, pfINb, pfINc, DataLen);
    memset(pfOUT, 0, DataLen * sizeof(float));
    for (const auto& fx : g_Core.GetFractals()) {
        if (fx.index >= 0 && fx.index < DataLen) {
            pfOUT[fx.index] = (float)fx.type;
        }
    }
}
```

---

#### Step 1.3 完成后验证

```
□ 编译通过（chan.dll + test_chan_core.exe）
□ 50 个单测全部通过
□ tdx_standard.cpp 中搜索以下关键词全部为 0 结果：
    RemoveInclude（函数定义，不含注释）
    CheckFX（函数定义）
    CheckBI（函数定义）
    CheckZS（函数定义）
    GetBiSequence（函数定义）
    CheckFirstBuy（函数定义）
    struct MergedKLine
    struct Fractal
    struct Stroke
    struct Pivot
□ tdx_standard.cpp 中 Stroke 等类型全部来自 #include，无本地 struct
□ 启动通达信，加载缠论主图_chan，笔/中枢/买卖点显示正常
□ chan_debug.log 显示 v7.5 + 配置参数
```

```bash
git tag v7.5-phase1 -m "架构收敛完成，算法统一到 chan_core.cpp"
```

---

## Phase 2：视觉修正（P1 级，修改备用件）

> **以下修改的都是备用件，不部署到生产。目的是确保备用件也处于可用状态。**

### Step 2.1 — 修复备用件买卖点锚定

**操作文件**：`formulas/缠论完整指标.txt`、`formulas/缠论买卖点.txt`

在两个文件中都添加锚点变量（如果还没有）：

```tdx
KXG:=TDXDLL1(20, H, L, C);
KXD:=TDXDLL1(21, H, L, C);
BPOS:=IF(KXD>0, KXD*0.998, L);
SPOS:=IF(KXG>0, KXG*1.002, H);
```

然后批量替换：
- 所有 `L*0.98` / `L*0.97` / `L*0.96` → `BPOS`
- 所有 `H*1.02` / `H*1.03` / `H*1.04` → `SPOS`

---

### Step 2.2 — 修复备用件中枢画法

**操作文件**：`formulas/缠论完整指标.txt`、`formulas/缠论中枢.txt`

#### 缠论完整指标.txt

删除：
```tdx
STICKLINE(在中枢内, ZSZG, ZSZD, 4, 1), COLORGRAY;
```

替换为（参照 `缠论主图_chan.txt` 的边框画法）：
```tdx
ZS_DIR:=TDXDLL1(22, H, L, C);
ZSKS:=TDXDLL1(18, H, L, C);
ZSJS:=TDXDLL1(19, H, L, C);
ZZ:=(ZSZG+ZSZD)/2;

{上涨中枢边框 — 红色}
STICKLINE(在中枢内 AND ZS_DIR>0, ZSZG, ZSZG, 0.2, 0), COLORRED;
STICKLINE(在中枢内 AND ZS_DIR>0, ZSZD, ZSZD, 0.2, 0), COLORRED;
STICKLINE(ZSKS=2, ZSZG, ZSZD, 0.1, 0), COLORRED;
STICKLINE(ZSJS=2, ZSZG, ZSZD, 0.1, 0), COLORRED;

{下跌中枢边框 — 绿色}
STICKLINE(在中枢内 AND ZS_DIR<0, ZSZG, ZSZG, 0.2, 0), COLOR1D7300;
STICKLINE(在中枢内 AND ZS_DIR<0, ZSZD, ZSZD, 0.2, 0), COLOR1D7300;
STICKLINE(ZSKS=1, ZSZG, ZSZD, 0.1, 0), COLOR1D7300;
STICKLINE(ZSJS=1, ZSZG, ZSZD, 0.1, 0), COLOR1D7300;

{中轴虚线}
STICKLINE(在中枢内, ZZ, ZZ, 0.1, 0), COLORDARKGRAY;
```

#### 缠论中枢.txt

注释掉方案 B（填充式），启用方案 C（边框式）。

---

### Step 2.3 — 公式隔离验证

通达信里 **只保留 `缠论主图_chan`** 作为主图公式，确认：
- 笔线正常（黄色连线）
- 中枢为边框（红/绿矩形框）
- 无黑块遮挡
- 买卖点位置合理

```bash
git tag v7.5-phase2 -m "视觉修正完成"
```

---

## Phase 3：维护收口（P2 级）

### Step 3.1 — 统一版本号

| 位置 | 当前值 | 改为 |
|------|--------|------|
| `tdx_standard.cpp` 文件头注释 | `v6.0 (2026-02-01)` | `v7.5 (2026-03-10)` |
| `dllmain.cpp` DllMain 日志 | `版本: 1.0.0` | `版本: 7.5.0` |
| `RegisterTdxFunc` 日志 | `v7.4 - 新增中枢方向输出` | `v7.5 - 统一算法架构` |
| `缠论完整指标.txt` 头注释 | `v7.4` | `v7.5` |
| `缠论买卖点.txt` 头注释 | `v7.4` | `v7.5` |
| `缠论中枢.txt` 头注释 | `v7.4` | `v7.5` |
| `缠论主图_chan.txt` 头注释 | （无版本号） | 添加 `{缠论主图 v7.5}` |

---

## Phase 4：发布闸门（必须通过才能上线）

### Step 4.1 — 黄金样本回归比对

1. 用新编译的 `chan.dll` 替换通达信中的旧版
2. 打开 999999 日线 和 600519 5分钟图
3. 用与 Step 0.1 相同的方法导出数据到 CSV
4. 对比新旧 CSV：

```powershell
# 简易 diff（Windows PowerShell）
Compare-Object (Get-Content baseline_999999_daily.csv) (Get-Content new_999999_daily.csv)
```

5. 如果有差异行，逐行判定：

| 判定 | 处理 |
|------|------|
| 已知改善（如缓存 bug 导致旧版漏算） | 记录到 `CHANGELOG`，放行 |
| 意外回退（新版丢失了旧版正确的笔/中枢/信号） | **阻塞发布**，定位修复后重新回归 |
| 不确定 | 保守处理为阻塞，排查后再判 |

### Step 4.2 — 回滚验证

在测试环境中模拟一次回滚操作：

1. 部署 v7.5 DLL → 确认正常
2. 用 `backup/chan_v74.dll` 覆盖 → 确认恢复到 v7.4 行为
3. 再次部署 v7.5 DLL → 确认正常

确保回滚路径可执行。

### Step 4.3 — 打正式发布 tag

```bash
git tag v7.5-final -m "生产发布版本，回归测试通过"
git push origin v7.5-final
```

---

## 全流程最终验证清单

```
Phase 0:
  □ 基线 CSV 已保存到 test/baseline/
  □ Git tag v7.4-final 已打
  □ DLL 已备份到 backup/

Phase 1:
  □ chan.dll 编译成功（链接 chan_core.cpp）
  □ test_chan_core.exe 50/50 通过
  □ tdx_standard.cpp 中无算法函数体、无本地 struct 定义
  □ chan_debug.log 显示 v7.5 + 实际配置参数
  □ 修改 CZSC.ini min_bi_length，重启后笔数变化可观测
  □ 实时行情下尾 bar 跳动时笔/中枢跟随更新
  □ Git tag v7.5-phase1 已打

Phase 2:
  □ 备用件锚点和中枢画法已修正
  □ 通达信只加载 缠论主图_chan，中枢为边框，无黑块
  □ Git tag v7.5-phase2 已打

Phase 3:
  □ 所有源文件/公式/日志版本号为 v7.5

Phase 4 (发布闸门):
  □ 999999 日线回归通过
  □ 600519 5分钟回归通过
  □ 所有偏差项已判定并记录
  □ 回滚验证通过
  □ Git tag v7.5-final 已打
  □ 正式发布件清单确认：chan.dll + CZSC.ini + dlls.ini + 缠论主图_chan.txt
```
